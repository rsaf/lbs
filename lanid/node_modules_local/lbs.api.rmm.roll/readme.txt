test depends on the cmm, to get that dependency without having to re install it you can softlink it:
in dev/bitbucket/lbs.api.rmm.yourbranche/test/node_modules use the following command:
ln -s ../../../lbs.api.cmm.development/ cmm

Assumint you have the cmm development branche checked out.

