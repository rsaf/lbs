leonard tchuta
netstat -nlt | grep '3000\|6379'
ss -nlp|grep redis
redis-cli ping
rpm -q iptables
lsmod | grep ip_tables


{
  "name": "cmm",
  "description": "Configurations Management Module",
  "version": "1.0.0",
  "main": "./cmm_Interface.js",
  "private": true,
  "dependencies": {
    "mongoose": "3.8.21",
    "q": "^1.1.2",
    "fs-extra": "^0.13.0",
    "node-uuid": "^1.4.2",
    "ssh2": "^0.3.6",
    "sequelize": "^2.0.0-rc8",
    "pg": "^4.2.0",
    "pg-hstore": "^2.3.1",
    "redis": "^0.12.1"
  }
}



[2/17/15, 3:10:03 PM] rolland safort ntague: #BASH FILE TO AUTOMATICALLY SETUP FILE STORAGE BUCKET AND UPLOADBOT USER FOR LOCAL TESTING OF PHOTOS, FORMS, FILES UPLOAD

echo 'whoami '
whoami
echo 'swiching current user to root user.. '
sudo su
whoami
echo 'generate uploadbot .ssh key pays'
rm -rf ~root/.ssh/upload*
ssh-keygen -f ~root/.ssh/uploadbot

whoami
echo 'creating uploadbot .ssh folder'
rm -rf ~uploadbot/.ssh
mkdir ~uploadbot/.ssh
chown uploadbot ~uploadbot/.ssh
chmod 700 ~uploadbot/.ssh

whoami
echo 'copying uploadbot.pub from user folder to uploadbot authorized keys'
sudo cp ~root/.ssh/uploadbot.pub ~uploadbot/.ssh/authorized_keys_temp

PK_TEMP_CMD='command="~/bin/main.sh"'
echo -n $PK_TEMP_CMD "" > ~uploadbot/.ssh/authorized_keys
echo 'the current value of authorized keys after echoing the PK_TEMP_CMD into it..'
cat ~uploadbot/.ssh/authorized_keys

cat uploadbot/.ssh/authorized_keys_temp  >> uploadbot/.ssh/authorized_keys

echo 'showing the content of authorized keys '
cat ~uploadbot/.ssh/authorized_keys

sudo chown uploadbot:staff ~uploadbot/.ssh/authorized_keys
sudo chmod 600 ~uploadbot/.ssh/authorized_keys
ls -al ~uploadbot/.ssh/authorized_keys

whoami
echo 'testing ... ssh to uploadbot@localhost '

ssh -i ~root/.ssh/uploadbot uploadbot@localhost
