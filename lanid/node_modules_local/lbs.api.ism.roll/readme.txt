
exports.testReadPersonalProfileByUserID = function(test){
    var m2 = {ns:null, op:null, pl:null};
    m.pl = {
    "userAccountID": '642d0eff3748330000dc5639'};
   // var paramPersonalJsonInput = JSON.parse(JSON.stringify(personal));
    oUPM.readPersonalProfileByUserID(ms)
    .then(function(r){
    console.log(r.pl);
    test.expect(2);
    test.equals(r.er, null);
    test.equals(m.pl.userAccountID,r.pl.userAccountID);
    test.done();
    });
};

exports.testUpdatePersonal = function(test){
    var m = {ns:null, op:null, pl:null};
    m.pl = {
    "userAccountID": "642d0eff3748330000dc5639",
    "lzID" : "002d0eff3748330000dc5632",
    "completion": 50, //completion Percent
    basic:{
        names: {"value": "Awang Bang", "verified":true, "verifiedDate":  Date.now()},
        gender: {"value": "Male", "verified":false, "verifiedDate": null }
    }};

   // var paramPersonalJsonInput = JSON.parse(JSON.stringify(personal));

    oUPM.updatePersonalProfile(m)
   .then(function(r){
    console.log(r.pl);
    test.expect(5);
    test.equals(r.er, null);
    test.equals(m.pl.userAccountID,r.pl.userAccountID);
    test.equals(m.pl.completion,r.pl.completion);
    test.equals(m.pl.basic.names.value,r.pl.basic.names.value);
    test.equals(m.pl.basic.gender.value,r.pl.basic.gender.value);
    test.done();
    });
};



