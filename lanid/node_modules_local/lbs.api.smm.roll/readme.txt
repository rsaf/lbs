test depends on the cmm, to get that dependency without having to re install it you can soft link it:
in dev/bitbucket/lbs.api.wmm.yourbranche/test/node_modules use the following command:
ln -s ../../../lbs.api.cmm.development/ cmm

Assume that you have the cmm development branche checked out.
