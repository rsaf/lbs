
var lib = require("lib");
module.exports = function (paramMongoose){
var serviceTypeSchema = paramMongoose.Schema({
    s : lib.permissionsSchema({all:"r"}),
  id:Number
  ,text:String
  ,order:Number
});

//Model Declaration ===============================

//public Methods ===============================
serviceTypeSchema.statics.getServiceTypesOrdered = function(paramCallback){
    return this.find({}).sort('order').exec(paramCallback);
};
return serviceTypeSchema;

};