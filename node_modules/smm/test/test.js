/* 
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

var httpRequest = require('request');
var hostAddress = 'http://localhost';

/*

exports.testRegisterUser = function(test) {
    var paramBody = {
        'loginName': 'wangke2',
        'password': '123456',
        'userType': 'personal',
        'antiBotValue': 'ABCDEF',
        'user_captcha': 'ABCDEF'
    };
    httpRequest.post( hostAddress + '/home/registration',paramBody, function(paramError, paramResponse, paramBody) {
        console.log(paramBody.toString());
        test.expect(2);
        test.equals(paramError, null);
        test.equals(paramResponse.statusCode, 200);
        test.done();
    });
   
};


exports.testLoginUser = function(test){
 var paramBody = {
        'loginName': 'wangke',
        'password': '123456',
        'userType': 'personal',
        'antiBotValue': 'ABCDEF',
        'user_captcha': 'ABCDEF'
    };
    
    httpRequest.post(hostAddress + '/home/login', function(paramError, paramResponse, paramBody){
    test.expect(2);
    test.equals(paramError, null);
    test.equals(paramResponse.statusCode, 200);
    test.done();
    });
};

*/

exports.testUserIsAuthorized = function(test) {
    httpRequest.get(hostAddress + '/personal/HomePageBusinessTask', function(paramError, paramResponse, paramBody) {
        console.log(paramBody.toString());
        test.expect(2);
        test.equals(paramError, null);
        test.equals(paramResponse.statusCode, 200);
        test.done();
    });
};
